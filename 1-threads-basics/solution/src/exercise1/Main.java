package exercise1;

public class Main {

    public static void main(String[] args) {
        System.out.printf("Id: %s, Name: %s", Thread.currentThread().getId(), Thread.currentThread().getName());
    }
}
